services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      DEBUG: "0"
      ALLOWED_HOSTS: "localhost,127.0.0.1"
      SECRET_KEY: "change-me-for-deploy"
      SQLITE_PATH: "/app/data/db.sqlite3"
    volumes:
      - ./.docker-data:/app/data
